# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem

# local tooling config (not part of the website project)
.claude/

# original raw video backups (kept locally, not needed to run the site —
# the site only ever loads the optimized .mp4 versions in this folder)
public/*.mov

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts
